package com.maplecheater.domain.repository.cheatingtype;

import com.maplecheater.domain.entity.CheatingType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CheatingTypeRepository extends JpaRepository<CheatingType, Long> {
}
